# AMM Architecture

- **Pricing Rule**: Constant product formula (`x * y = k`)
- **Tokens**: Pool supports trading between `token0` and `token1`
- **LP Shares**: LP shares minted on liquidity deposits and burned on withdrawal
